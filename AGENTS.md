# Smart Badge 项目工作规则

本文件适用于本仓库内工作的主智能体和子智能体。目标是在理解现有业务链路和模块边界的基础上，以最小、可验证、可追溯的改动完成任务，并保持架构与文档体系干净。

## 一、最高优先级

- “二、文档读取与写入门禁”和“五、多智能体编排”是最高优先级规则，不得被其他章节削弱或绕过。
- 不得以任务简单、改动很小、时间不足、已有聊天记录、已有提交说明或已由子智能体处理为由跳过被触发的门禁。
- 只实现有明确依据的业务规则；不得根据猜测增加功能、状态、接口、抽象、兼容、兜底、降级或无关重构。

## 二、文档读取与写入门禁

项目文档总入口为 `docs/README.md`，文档职责、权威边界、内容要求和 Bug 追查方法以 `docs/governance/document-architecture.md` 为准。普通 `docs/*.md` 不会自动成为执行指令；主智能体必须按本节主动读取，并把适用规则传给子智能体。

### 2.1 何时读取

需要理解或改变项目行为的任务，包括需求、开发、Bug、审查、测试、合并、冲突处理、部署和回滚，在制定方案或写入项目前必须先读 `docs/README.md`，再按任务定向读取；不得全量扫描文档：

- 新增、修改或删除行为：读取现行需求索引、相关 `REQ-xxx`、架构、契约和对应代码证据。
- Bug、现场异常、回归或数据问题：读取问题所在运行版本及部署记录、相关需求/开发/合并记录、当前架构与契约，再核验代码、迁移、配置、测试和必要运行证据。
- 合并、cherry-pick、rebase 或冲突处理：读取共同基线、两侧关联的需求与开发记录、相关架构与契约，分别提取两侧不可丢失的行为。
- 测试设计或测试链路变化：读取 `docs/testing` 和 `docs/operations/testing.md` 的相关部分。
- 部署、回滚、迁移或恢复：读取 `docs/operations` 中对应的当前手册、环境边界及相关部署记录。
- 创建或更新任一文档前：读取 `docs/governance/document-architecture.md` 中与该文档类型和当前任务相关的章节。

`docs/history` 只在追溯历史、旧版本或来源时参考；`docs/working` 只在用户明确指定、权威文档引用或继续同一调查时参考。二者均不能直接裁决当前需求或当前实现。

### 2.2 何时写入

以下时点固定，不得改为边操作边写，也不得拖到验收之后：

1. **需求记录：实施前。** 允许先做最小必要的只读调查；一旦确认任务将新增、修改、修复或删除项目行为、代码、配置、脚本、测试、迁移、接口、Schema、构建或部署实现，或者将执行合并或冲突处理，必须在首次项目写入前找到或创建当天需求记录及索引。需求未落盘，不得实施。
2. **开发记录、合并记录和当前事实文档：实现与验证结束后、验收前。** 不在操作过程中持续回填。实现、调整和相关验证全部结束后，一次性写入本次开发记录；发生合并或语义整合时同时写入合并记录；并更新所有因实际结果而失真的当前事实文档及索引。
3. **部署记录：部署操作结束后、验收部署前。** 实际部署、回滚或既有部署流程失败后，一次性记录真实环境、版本、过程、结果、异常和未解决事项。

当前事实变化按实际影响更新：模块职责、业务链、身份、状态、消费者或运行边界写入 `docs/architecture`；公共 API、event、job、稳定 ID、状态、权限、幂等或外部 payload 写入 `docs/contracts`；长期测试策略和证据边界写入 `docs/testing` 或 `docs/operations/testing.md`；当前部署、迁移、回滚、恢复或维护流程写入 `docs/operations`；权威入口、目录职责或导航变化写入 `docs/README.md` 或相关索引。

开发记录、合并记录和所有被实际变化触发的当前事实文档，必须在同一验收阶段完成并与最终代码、配置、迁移、测试和运行结果一致。缺少任何应写项、索引未更新或内容与实际结果不一致，均不得验收，也不得声明“已完成”“已修复”或“已交付”。

仅咨询、解释、状态查询、只读审查、只读诊断，或只整理不改变项目行为和业务口径的说明文档，不触发需求和开发记录。仅提交或推送已有工作区内容时也不创建这些记录，只核对现有需求并写准确的提交备注。若过程中需要修改实现，立即切换为变更任务并执行上述门禁。

为远端推送、部署、合并或冲突处理补写的本地验收记录，默认不纳入该次远端操作，也不得仅为同步这些记录再次提交或二次推送；用户当前任务明确要求同步文档时除外。

## 三、工程边界

- 修改前按任务相关性核验入口、数据流、状态、事务、权限、重试、幂等、异步消费者、外部系统及上下游契约；不得把“理解链路”机械扩张为全仓库调查。
- 权威需求定义目标行为；架构、契约和代码用于核验当前事实。发生冲突时记录差异，不得把代码现状自动解释为正确需求。
- 优先复用现有模块、接口和扩展点。未理解相关链路、模块职责和影响范围前，不得新增模块、服务、状态机、配置体系或功能入口。
- 修改限于当前需求所需的最小合理范围。发现范围外问题时记录事实和影响，不得顺带修改。
- 后发用户原话或纠正优先于旧口径；无法通过证据裁决的关键业务问题，由主智能体请求用户确认。

## 四、Bug、回归与合并

- Bug、回归、复盘、修复、合并和冲突处理必须读取并遵循 `docs/governance/document-architecture.md` 的对应章节。
- 旧需求、开发、部署和合并记录只用于还原当时输入、理解、选择和验证边界，不是必然正确的答案；不得事后无痕改写旧记录。
- 修复或合并前必须重建原改动目的、不可丢失能力、验证边界及受影响依赖；最终验证原需求、新问题、两侧能力和必要的组合场景。

## 五、多智能体编排（高优先级）

- 主智能体负责理解需求和系统、设计架构与实现逻辑、确定接口和数据契约、拆分并分配任务、处理集成问题并完成最终验收。主智能体应先决定代码应当如何设计和运行，再将方案转化为边界清晰、可独立执行且验收标准明确的任务。
- 代码搜索、机械读写代码和文档、按既定方案实现、批量修改以及目的明确的测试等执行工作，由子智能体完成。主智能体可以读取作出架构决策和最终验收所需的关键内容。
- 存在多个互不依赖的执行任务时，主智能体可以同时委派给不同子智能体，最多并发三个。每个任务应明确实现范围、既定设计和验收标准；同一执行域出现后续工作时，优先续用原子智能体。
- 子智能体默认使用 `gpt-5.6-luna`，推理强度为 `medium`；只有用户当前任务明确指定时才使用其他模型。
- 委派时传递与子任务相关的用户原话和最新纠正、目标、范围、既定设计、禁止事项、允许写入位置、依赖、交付物、验收标准和适用规则；不得假定子智能体能够看到主对话。
- 子智能体按主智能体确定的方案完成实现和测试，并返回改动及验证证据；任务中尚未确定的架构、跨模块逻辑或接口契约，由主智能体裁决。子智能体可以报告范围外发现，但不得自行扩大实施范围。
- 子智能体完成后，主智能体最后审查完整工作区、最终差异和验证证据，处理集成问题，执行必要的最终验证并作出验收结论。

## 六、上下文安全

- 不得在一次请求或工具调用中读取、写入或返回大量文本；长文本、大量文档、日志和补丁必须分批处理，必要的详细材料落盘，主上下文只保留摘要和精确定位。
- 多个子智能体不得一次性集中返回大量内容；应分批回传，详细材料落盘。

## 七、测试与验收

- 开发时运行 `npm test` 或 `npm run test:dev`，并补充必要的受影响功能测试；不默认运行全量套件。
- `npm run test:submit` 仅在从当前工作区创建 Git 提交前，或分支合并完成后验收最终合并结果时运行。
- 其他开发验收按上一条执行。
- 测试失败先修复；无法运行时说明原因、替代检查和剩余风险。
- 定向 Python 测试使用 `npm run test:target -- <pytest-node-id>`，由 runner 自动选择离线或专用 PostgreSQL 测试配置。
- 验证范围与实际改动、风险和依赖相匹配，优先定向测试和受影响链路检查；仅在公共接口、共享依赖、构建、迁移、并发或高风险链路受影响时扩大范围。
- 不新增或运行与当前需求和风险无关的测试，不以形式覆盖率制造无行为价值的测试。无法执行相关验证时说明原因、替代检查和剩余风险。
- 未经用户明确授权，不得部署生产、执行生产迁移、修改生产业务数据或启停生产服务。
- 最终验收前，主智能体必须核对：用户要求和验收口径已满足；架构及链路已按需核验；验证与风险匹配；子智能体产出已整合；所有被触发的需求、开发、合并、部署和当前事实文档及索引均在规定时点完成并与实际结果一致；未完成事项和风险已明确说明。
