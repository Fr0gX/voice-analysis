# 仓库级 AGENTS.md 模板

本文件规定仓库级协作方式。目标是在理解现有业务链路和模块边界后，以范围清楚、可验证、可追溯的改动完成任务。

## 复用配置：复制后首先修改

项目专有的路径、命令和运行参数只允许出现在本节。复制到其他仓库后，先逐项替换并确认路径真实存在；正文使用配置项名称引用这些值。

| 配置项 | 当前仓库值 |
| --- | --- |
| 文档入口 | `docs/README.md` |
| 文档规范 | `docs/governance/document-architecture.md` |
| 文档治理范围 | 启用需求、开发、合并、部署过程记录，以及架构、契约、测试、操作和导航等当前事实文档 |
| 测试知识 | `docs/operations/local-development.md` |
| 测试手册 | `docs/operations/local-development.md` |
| 环境手册 | `docs/operations/local-development.md` |
| 部署手册 | 尚未建立；当前边界与待开发项见 `PROJECT_STATUS.md` |
| 开发自检命令 | `./scripts/test-services.ps1` |
| 精确定向测试命令 | `.venv-embedding/Scripts/python.exe -m pytest <pytest-node-id>` 或 `.venv-refine/Scripts/python.exe -m pytest <pytest-node-id>` |
| 合并验收命令 | `./scripts/test-services.ps1` |
| 子智能体默认模型 | `gpt-5.6-luna` |
| 子智能体推理强度 | `medium` |
| 子智能体并发上限 | `3` |
| 本文件管理方式 | 随仓库版本管理 |

具体业务事实和操作步骤由“文档入口”指向的权威资料说明，不在本文件重复维护。

## 一、工作原则

- 以用户最新的明确要求为目标，以现行需求裁决业务口径，以架构、契约、代码、迁移、配置、测试和必要的运行证据核验当前事实。材料冲突时说明差异，不把代码现状或旧记录自动视为正确答案。
- 在证据足以说明相关链路如何工作、改动边界和可能影响之前，不修改实现。调查覆盖可靠设计和验证所需的范围，不以全仓扫描代替理解。
- 优先使用现有模块、接口和扩展点，只实现需求所需的最小合理改动。新增业务规则、兼容、兜底、降级、抽象、模块或功能入口都应有明确依据。
- 范围外发现记录事实和影响，留给用户决定；当前任务不顺带修改。无法从证据确定的关键业务问题由主智能体向用户确认。
- 保留用户已有的工作区改动。提交、合并、清理或回退前先核对准确目标，不覆盖与当前任务无关的内容。

## 二、文档的读取与写入

“文档入口”是项目文档总入口；文档职责、权威边界、写法和 Bug 追查方法以“文档规范”为准。

### 开始工作时读取

凡是需要理解业务、代码、Bug、测试、合并或部署的任务，先读“文档入口”，再按任务定向读取相关资料：

- 行为变更：现行需求索引、相关需求记录、架构、契约和代码证据。
- Bug、回归、现场异常或数据问题：问题所在版本和部署记录、相关需求/开发/合并记录、当前架构与契约，以及必要的代码、迁移、配置、测试和运行证据。
- merge、cherry-pick、rebase 或冲突处理：共同基线、两侧需求与开发记录、相关架构和契约，并读取“测试手册”。
- 测试设计或测试链路变化：“测试知识”和“测试手册”中相关部分。
- 部署、回滚、迁移或恢复：“环境手册”“部署手册”及相关当前操作手册和部署记录。
- 创建或更新文档：先读“文档规范”中对应文档类型的要求。

历史资料只用于追溯，短期工作资料只用于用户明确指定、权威文档引用或继续同一调查的场景。两者都不能单独裁决当前需求或当前实现。

### 在固定时点写入

会改变项目行为、当前事实或业务语义的工作必须留下与最终结果一致的记录；合并、冲突处理和实际部署分别留下对应记录。写入时点固定如下：

1. **需求记录在实施前完成。** 完成最小必要的只读调查后，在首次实现写入前找到或创建当天需求记录并更新索引。合并或冲突处理同样先记录两侧目标和必须保留的语义。
2. **开发、合并和当前事实记录在操作结束后完成。** 实现、调整和相关验证全部结束后，在验收前一次性写入开发记录；发生合并或语义整合时同时写入合并记录；实际结果改变了架构、契约、测试策略、操作方法或文档导航时，同步更新相应当前事实文档和索引。文档应与最终代码、配置、迁移和验证结果一致，缺少应写内容时不通过验收。
3. **部署记录在部署结束后完成。** 实际部署、回滚或既有部署流程失败并结束后，在部署验收前一次性记录真实环境、版本、过程、结果、异常和未解决事项。

记录真实形成的事实，不在操作过程中持续回填，也不把后来才知道的结论改写成当时已经明确。详细写法、日期、编号和索引规则统一遵循“文档规范”。

不改变项目行为、当前事实或业务语义的只读工作和文字整理不产生过程记录。只提交或推送已有工作区内容时，不新建需求或开发记录，只核对对应需求并写准确的提交备注；过程中发生实现变更则按变更任务处理。仅调整由“本文件管理方式”声明为 Git 忽略的本地协作配置，也不产生项目记录。

为远端推送、部署、合并或冲突处理补写的本地验收记录默认留在本地，不为同步这些记录再次提交或推送；用户明确要求同步文档时除外。

## 三、Bug、回归与合并

追查、复盘或修复问题时，旧需求、开发、部署和合并记录用于重建当时的输入、理解、假设、实际改动和验证边界，而不是提供必然正确的答案。结合当前事实确定问题首次出现的版本、原改动要解决的问题、不能丢失的能力、后来被证明不完整的假设，以及已经依赖当前行为的模块和消费者。修复应同时验证原需求、新问题和必要的组合场景。

历史记录保留形成时的语境。后续纠正使用追加说明或新记录表达覆盖关系，不无痕改写旧证据；未执行的验证如实写为未验证。

合并先确定两侧分别要保留的业务语义，再决定冲突代码如何实现。除文本冲突外，还要检查接口、状态、权限和消费者之间的隐性语义冲突。最终结果应保留两侧不可丢失能力，并完成两侧验收和必要的组合验证。

合并和验证应保护用户已有改动，并在任务结束时清理本次创建的临时环境；具体方法按“测试手册”执行。

## 四、多智能体协作

多智能体协作的目标是让主智能体集中于需要全局上下文和高强度判断的工作，同时并行完成可以独立核验的辅助工作。

主智能体始终负责需求与系统理解、架构和接口设计、业务逻辑、实现方案、全部代码修改、跨模块集成以及最终验证和验收。子智能体不承担这些决策，也不修改源代码、测试代码、脚本、迁移、配置或其他影响系统实现的文件。

代码与文档调查、调用链梳理、测试执行、故障取证、专项审查和文档整理等工作，在边界清楚、结果可独立核验时交给子智能体。子智能体可以按主智能体确认的事实和口径修改明确指定的文档；超出既定范围或需要实现判断时，返回主智能体裁决。

存在多个互不依赖的辅助方向时，在“子智能体并发上限”内并行委派；同一方向继续调查或复核时，优先续用已有子智能体的上下文。子智能体使用“子智能体默认模型”和“子智能体推理强度”，用户明确指定其他设置时除外。

每次委派都提供足以正确执行的当前上下文，明确目标、边界、允许写入的文档和验收依据，并包含相关的用户原话和最新纠正。子智能体返回精确位置、事实、验证结果或文档差异；主智能体核对证据、整合结果，并以完整工作区为对象作出最终判断。

## 五、上下文与输出

为保持主上下文稳定，调查采用渐进式读取：先定位与任务相关的文档章节、代码符号和文件，再只读取当前判断需要的部分。完整长文件、宽泛搜索结果、测试日志和多个子智能体的详细返回不集中进入一次请求；需要完整保存的材料分批处理或落盘，主上下文只保留摘要、精确位置和下一步。

同一错误连续出现两次时，先分析根因并改变代码、配置、环境或方法，再继续；等待子智能体或后台进程时使用合理间隔。

## 六、测试与验收

验证范围与实际改动和风险相匹配：开发阶段使用“开发自检命令”和必要的定向测试，影响扩大时再验证相关公共链路；不以无关测试增加表面完整性。需要精确回归时使用“精确定向测试命令”。

“合并验收命令”只用于分支合并完成后的最终合并树，并按“测试手册”规定的环境和运行顺序执行一次。普通提交、单纯推送已有提交、完成开发、普通验收或向用户交付结果都不触发该全量验收，用户明确要求时除外。

主智能体以完整工作区和真实验证证据验收。验收结论应与用户要求、最终差异、相关链路、文档记录、失败归属、未验证事项和剩余风险一致；局部测试或子任务完成不能替代最终验收。

## 七、部署与生产边界

部署和回滚以“环境手册”“部署手册”及对应环境的当前操作手册为准。发布阶段使用用户指定的完整目标提交，把上游已经完成的代码测试作为输入，不在发布阶段重复运行；环境健康检查和冒烟属于部署结果验证，不能由代码测试替代。

生产部署、生产迁移、生产业务数据修改以及生产服务启停都需要用户明确授权。实际操作只触及当前任务指定的环境和资源，并在结束后写入真实部署记录；未执行或失败的步骤不记录为成功。
