# Voice Analysis 项目现状与开发需求

状态：M1 已验收；M2 最小异步任务 API 已实现；M3 双入口 Web 与腾讯/阿里云 ASR 已实现并通过本地自动测试，真实云凭据联调待执行

核验日期：2026-09-01

## 1. 产品目标

目标输入：一份原始录音，以及由调用方提供的 ASR 句段文字和毫秒级开始、结束时间。

目标输出：保留全部原始文字和时间戳，并为每个句段增加单次录音内的本地说话人标签、归属置信度、归属依据和未知状态。说话人标签不表示人员身份，不跨录音复用。

计划中的产品链：

```text
Web 页面
  → 上传原始录音和 ASR 句段
  → 统一编排 API
      → 8078 pyannote 候选窗口细化
      → 窗口质量评分和高质量窗口选择
      → 8077 ECAPA 句段/窗口向量
      → 说话人数估计和录音内聚类
      → 完整 ASR 句段归属
  → 同步播放、查看和导出说话人分离转写
```

## 2. 当前项目结构

```text
voiceanalysis/
  .env                         本机密钥与运行参数；Git 忽略
  .env.example                 无密配置模板
  config/
    services.yaml              两个模型服务和运行目录配置
    analysis.yaml              M1 输入、组件、导出、资源和锁定算法配置
    model-manifest.json        模型来源、版本、大小和 SHA256
    evaluation.yaml            M1 基础评测规模、评分策略和验收门槛
  docs/
    requirements/              需求记录
    architecture/              当前系统边界
    contracts/                 当前 HTTP 契约
    operations/                本地环境和启动手册
    development/               实施与验证记录
    history/                   Smart Badge 历史来源
  environments/
    analysis-engine/           M1 轻量编排、聚类和评测依赖
    voice-embedding/           ECAPA 独立依赖和锁文件
    window-refine/             pyannote 独立依赖和锁文件
  runtime/
    models/                    本地模型权重；Git 忽略
    audio/                     本地输入音频；Git 忽略
    logs/                      本地服务日志；Git 忽略
    tmp/                       PID、临时产物；Git 忽略
  scripts/                     初始化、模型安装、启动、停止和验证脚本
  voice_analysis_engine/       M1 编排、算法、CLI、导出、评测和测试
  voice_embedding_service/     8077 SpeechBrain ECAPA 服务和测试
  window_refine_service/       8078 pyannote 窗口细化服务和测试
```

## 3. 当前资产

| 资产 | 当前状态 | 已验证事实 |
| --- | --- | --- |
| Python | 三套 CPython 3.11 隔离环境 | 模型环境相互隔离；分析环境不加载 Torch；依赖实际导入通过 |
| ECAPA | SpeechBrain 1.1.0、Torch/Torchaudio 2.11 | 模型加载成功；输出 192 维 L2 归一化向量 |
| pyannote | pyannote.audio 4.0.7、segmentation-3.0 | 模型加载成功；真实 `/segment` 推理成功 |
| 模型权重 | 位于 `runtime/models` | 10 个文件的大小和 SHA256 与清单一致 |
| 8077 | `/health`、`/health/ready`、`/embed` | ready 和真实 PCM 推理通过 |
| 8078 | `/health`、`/health/ready`、`/segment` | ready、双受控根目录、候选区分块随机读取和真实推理通过 |
| M1 引擎 | `AnalysisEngine`、`analyze/evaluate` CLI | 真实 8077/8078 闭环生成 JSON/TXT/SRT/VTT；ASR speaker 未参与算法 |
| 鉴权 | `X-Voice-Analysis-Key` | 两个业务接口都拒绝缺失或错误密钥 |
| 本地运行 | 前台、联合启动、停止、日志和 PID 脚本 | 两个进程联合启动、等待 ready、停止通过 |
| 测试 | embedding、refine 及 M1 引擎测试 | embedding 16 passed、1 skipped；refine 6 passed；engine 36 passed |

模型权重由相邻 Smart Badge 历史资产恢复到本地忽略目录。仓库跟踪的 [`config/model-manifest.json`](config/model-manifest.json) 记录来源提交、文件大小和 SHA256，可重复验证或重新安装。默认关闭的旧 OSD pipeline 没有迁入权重；主链使用 segmentation-3.0 powerset 输出，不依赖旧 OSD。

## 4. 已具备能力

### 4.1 语音窗口细化

调用方提供音频路径和 ASR 候选时间范围后，8078 能够：

- 将音频统一解码为 16 kHz 单声道 PCM；
- 在候选范围内判断语音、重叠和本地说话人变化；
- 生成连续单人语音候选窗口；
- 返回窗口质量信号和审计摘要。

### 4.2 说话人向量

调用方提供批量 PCM 窗口后，8077 能够：

- 校验模型版本、采样率、窗口边界、截止时间和请求大小；
- 对有效窗口进行有界队列和批量 ECAPA 推理；
- 逐窗口返回 192 维归一化向量或明确失败状态；
- 在模型、队列、协议或窗口失败时返回明确状态，不输出成功零向量。

### 4.3 M1 单录音分析

给定音频路径和 `voice_analysis_input_v1` 句段文件，CLI 能够：

- 按内容探测格式并单次规范化音频，后续通过受控临时 WAV 随机读取；
- 编排 8078、窗口评分、黄金窗口、8077、NME/加权 KMeans 和句段归属；
- 生成 `local_spk_n` 或 `unknown`、置信度、归属依据、风险与完整审计；
- 原子写入权威 `result.json`，并从它派生 TXT、SRT 和 VTT；
- 按 `config/evaluation.yaml` 评分 Manifest，数据不足时返回 `insufficient_dataset`。

## 5. 当前契约与后续产品能力

### 5.1 统一输入输出契约

M1 已通过独立 CLI 契约接收音频路径和 ASR 句段；M2 仍需在此 Schema 之上新增公开异步 API。首版规范句段字段为：

```json
{
  "id": "seg_001",
  "start_ms": 1200,
  "end_ms": 4300,
  "text": "您好，请问今天想咨询什么？",
  "confidence": 0.96
}
```

首版默认边界已经由 [`REQ-006`](docs/requirements/2026-08-31.md#req-006-m1-输入边界与可配置产品策略) 冻结：

- 支持 WAV/PCM、MP3、FLAC、M4A/AAC、OGG/Vorbis、Opus、WebM/Opus、AMR-NB/AMR-WB 和 3GP，按文件内容探测；
- 单文件非空且默认不超过 1 GiB，时长默认在 400 ms 至 4 h；
- 时间戳使用毫秒，默认稳定排序并保留输入序号，允许重叠，拒绝无效区间和越界；
- `id/start_ms/end_ms/text` 必填，重复 ID 拒绝，空文本保留并告警，缺失置信度保存为 `null`；
- 原始 ASR speaker 可以接收、保留和回显，但只能用于审计，不能参与算法；
- JSON 保存完整结果和审计元数据，TXT、SRT、VTT 默认以本地 speaker 或 `unknown` 前缀保留原文。

输入规范化、导出展示和失败策略已经进入 `config/analysis.yaml` 并允许覆盖；算法资产阈值在同一配置的锁定节中记录，覆盖时直接失败。

### 5.2 录音内聚类与句段归属

M1 已从固定历史快照中独立实现以下算法闭包：

1. 根据 ASR 时间轴建立候选区域；
2. 对 8078 返回窗口进行质量评分；
3. 选择高质量窗口并调用 8077；
4. 使用 normalized maximum eigengap 估计说话人数；
5. 使用质量加权 KMeans，必要时使用球面 KMeans 实现同等确定性；
6. 计算质量加权代表向量、medoid、核心半径和接受半径；
7. 为完整 ASR 句段提取向量；
8. 根据余弦距离、第二候选间隔和质量门完成句段归属；
9. 无法可靠归属的句段保留 `unknown`，不使用 ASR speaker 或人员身份填充。

实现没有迁入历史 Smart Badge 的 `app.core`、数据库、配置中心、任务队列、ASR provider、身份参考匹配或业务回写。大规模 NME 使用同一近邻图的稀疏求解路径，并与密集路径做重叠规模 parity。

### 5.3 异步任务 API

8076 已把 M1 封装为单机异步任务 API：

- 创建任务并上传输入；
- 查询 `queued/running/succeeded/failed/cancelled/expired`；
- 获取完整结果；
- 主动删除音频和结果；
- 可选幂等键、显式截止时间和尽力取消；
- 后端单执行槽原子准入，容量不足或模型未 ready 时返回 429，不在 M2 排队；
- 临时数据过期清理。

任务事实、输入和结果保存在受控本地任务目录；启动时遗留活动任务由后端顺序从头重跑。默认 24 小时删除业务资产，再保留 24 小时最小过期状态。数据库、多进程、多节点、断点续跑、自动重试和实时推送不在首版范围。

### 5.4 Web 前端

首版页面需要：

- 上传或选择原始音频；
- 上传或粘贴 ASR JSON；
- 在提交前校验并预览时间轴；
- 展示任务进度和明确错误；
- 音频播放与当前句段同步高亮；
- 不同本地说话人使用稳定颜色；
- 展示 unknown、置信度和风险信息；
- 下载 JSON、TXT、SRT 和 VTT。

说话人重命名、合并、拆分和人工修订可以作为后续阶段，不进入首版最小闭环。

### 5.5 数据与安全

需要在实现统一 API 前确定：

- 音频和结果默认保留时间；
- 删除任务是否立即清除音频、临时 PCM 和结果；
- 日志禁止记录音频正文、完整 embedding 和 API key；
- 上传文件名不能直接成为主机路径；
- 文件格式探测、解码资源限制和恶意输入保护；
- 模型、API 和算法版本如何进入结果元数据。

推荐首版：原始音频和结果默认保留 24 小时，允许主动删除，不永久保存 embedding。

### 5.6 评测与验收

当前测试可以证明组件协议、模型加载和真实录音闭环，但不能单独证明业务准确率。M1 冷启动验收门槛已经写入 [`config/evaluation.yaml`](config/evaluation.yaml)，评测执行器已经实现。相邻 Smart Badge 的“录音2”先构建过 20 个派生弱标注样本，后续又使用用户确认自然人分配准确的 12 份最终审核转写完成监督评测；两者都只有 12 个独立源录音，报告状态保持 `insufficient_dataset`，不能替代以下正式数据集：

- 合法下载 PriMock57、AISHELL-4 和 AliMeeting 等公开原始音频并生成本地评测清单；
- 至少 20 份、合计 60 分钟，并覆盖单人、双人、多人、重叠和噪声/远场场景的基础评测集；
- DER ≤ 0.30、JER ≤ 0.40、说话人数完全正确率 ≥ 0.80、已分配句段准确率 ≥ 0.85、非 `unknown` 覆盖率 ≥ 0.80；
- 模型、算法及权威阈值参数的版本快照和评测记录；首版不得以校准为理由自行改值；
- 长录音、并发、超时、内存和队列压力测试；
- 前端上传、轮询、同步播放和导出测试。

私有 ECAPA parity 语料没有迁入，因此对应历史测试当前跳过；它不再是 M1 基础评测的必要条件。若数据量或场景不足，评测必须报告 `insufficient_dataset`，不能判定通过。

“录音2”弱标注集共 8.241 小时、7,086 个句段；场景代理覆盖单人 3、双人 3、多人 14、重叠 17、噪声/远场 6。两次真实运行均为 20/20 `success`，聚合结果稳定为 DER 0.390140、JER 0.613331、人数完全正确率 0.35、人数 MAE 1.20、已分配句段准确率 0.638470、非 unknown 覆盖率 0.551146、unknown 比例 0.448854；七项正式门槛均未达到。该结果同时受弱标注质量影响，只作为当前模型基线和后续改进证据。

后续 12 份最终审核转写由用户确认不同标签在单份录音内准确对应不同自然人，标签名称的角色语义不参与评分。监督结果为：12/12 引擎 `success`；固定句段自然人错误率 0.492006、JER 0.735016、已归属句段准确率 0.510550、非 unknown 覆盖率 0.803653、unknown 比例 0.196347。按系统实际建立的全部簇复核后，人数完全正确率为 0.333333、人数 MAE 为 0.916667。主要错误来自自然人混淆，置信等级尚未校准，256 个不足 1.5 秒的句段全部拒识。自动评测负责计算，最终资格、指标语义和验收由监督判断裁决；完整证据见 [`REQ-010`](docs/requirements/2026-09-01.md#req-010-m1-监督质检与验收裁决) 和 [2026-09-01 开发记录](docs/development/2026-09-01.md#req-010-m1-监督质检与验收裁决)。

### 5.7 构建与部署

当前本机没有 Docker 命令。仓库已经迁入并改写两个 Dockerfile，但尚未执行容器构建。后续需要：

- 明确统一 API 和前端的进程拓扑；
- 决定模型作为挂载资产、release artifact 或 Git LFS 的交付方式；
- 增加 `.dockerignore`、容器编排、资源限制和非 root 用户；
- 验证 CPU 和可选 CUDA 镜像；
- 增加模型 ready、磁盘、队列和内存监控；
- 明确生产密钥、HTTPS、上传限制和数据清理操作手册。

## 6. 开发范围与里程碑

后续完整产品链收敛为三条开发主线：

1. Web 前端与交互体验；
2. 说话人分析编排与处理引擎；
3. 异步任务 API 与运行系统。

三条主线不表示实施依赖顺序。端到端交付按 M1 → M2 → M3 推进；统一 API、句段 DTO、任务状态和数据保留规则冻结后，前端可以使用契约 mock 与后端并行开发。

### 6.1 M1：说话人分析编排与处理引擎

实施状态：用户已验收通过。代码闭环、组件兼容、资源与安全边界、真实模型运行、版本化契约和可评测能力已完成；12 份自然人参照录音暴露的精度问题作为已知算法风险接受，不阻塞 M2。该里程碑验收不等同于 `m1_baseline_v1` 数值门槛通过，公开评测原始数据和完整场景覆盖仍待补充。

目标：把现有 8078 窗口细化和 8077 ECAPA 向量服务编排为确定性的单录音处理引擎。

权威基线：现有资产中的模型、服务限制、窗口质量、聚类、说话人数估计、簇代表、句段归属、拒识和风险召回阈值及参数均为 M1 首版权威初始值。实现只做原值提取、集中配置和版本记录，不自行重新选值或调参；来源优先级和变更边界见 [`REQ-005`](docs/requirements/2026-08-31.md#req-005-m1-权威资产参数与开发前人工决策)。

范围：

- 校验并规范化原始音频和 ASR 句段；
- 根据 ASR 时间轴建立候选区域并调用 8078；
- 计算窗口质量，选择可用于聚类的高质量窗口；
- 调用 8077 提取候选窗口和完整句段向量；
- 估计说话人数，执行录音内聚类并构建簇代表；
- 根据余弦距离、第二候选间隔和质量门完成全部 ASR 句段归属；
- 对无法可靠归属的句段保留 `unknown`；
- 生成带算法、模型和阈值版本的统一结果，并支持 JSON、TXT、SRT 和 VTT 导出。

输入、输出、失败和 ASR speaker 审计策略已有版本化默认值并允许配置覆盖，不再阻塞首次实现。系统性组件失败默认使整份分析失败；单个窗口或句段失败默认产生部分结果并将受影响句段保留为 `unknown`。基础评测规模、评分策略和业务门槛也已由 [`REQ-007`](docs/requirements/2026-08-31.md#req-007-m1-基础评测门槛配置化) 和 `config/evaluation.yaml` 冻结，不再等待人工补值。

交付物：独立于 HTTP 和任务存储的处理模块、可执行 CLI、固定输入输出 Schema、算法单元测试和真实模型端到端测试。

完成标准：给定一份合法音频和 ASR JSON，CLI 能保留全部原始句段、文字和时间戳，为每个句段输出本地说话人标签或 `unknown`、置信度和归属依据；任何组件或窗口失败都显式表达，不使用 ASR speaker 或人员身份伪装聚类结论。

### 6.2 M2：异步任务 API 与运行系统

实施状态：REQ-011 定义的单机最小版本已实现；API、存储、准入、生命周期和 M1 回归自动测试通过，真实 8076→M1→8078/8077 上传、轮询、四种下载和删除闭环通过。

目标：把 M1 处理引擎封装为可管理、可清理、可观测的异步任务服务，并形成稳定的前后端契约。

范围：

- 创建任务并安全接收音频和 ASR 输入；
- 查询 `queued/running/succeeded/failed/expired` 状态和最终结果；
- 支持请求幂等、截止时间、取消、主动删除和错误归属；
- 通过进程内后端原子准入；当前后端单执行槽且不建立新任务等待队列；
- 管理原始音频、规范化 PCM、任务状态、结果和临时文件；
- 实现默认过期时间和清理机制，不永久保存 embedding；
- 提供轮询接口；确有实时体验需要时再增加 SSE，不把 WebSocket 作为首版前提；
- 暴露健康、模型 ready、后端占用、保留任务和失败摘要。

交付物：任务 HTTP API、本地任务状态、进程内分析后端、清理器、OpenAPI/Schema 和生命周期测试。前端 mock 留给 M3 接入时按当前契约生成。

完成标准：任务从提交到成功、失败、取消、过期和删除的状态转换可重复验证；进程重启、超时、队列过载、单模型失败和清理失败均有明确语义；未授权请求无法读取或删除任务数据。

### 6.3 M3：Web 前端与交互体验

实施状态：REQ-012 的本机 React Web、现成转写与云 ASR 双入口、腾讯 Flash ASR、阿里 NLS、临时凭据内存边界、同步复核和四种导出已实现；组件测试、生产构建及本地浏览器路由验收通过。因未提供腾讯和阿里测试凭据，两个真实云账号闭环尚未执行，不能宣称真实供应商联调通过。

目标：提供面向使用者的完整上传、处理、复核和导出闭环。

范围：

- 上传或选择原始音频，上传或粘贴 ASR JSON，或单次提供腾讯/阿里凭据先转写；
- 提交前校验字段、时间单位、排序、重叠和音频边界，并预览时间轴；
- 创建任务，展示排队、运行、成功、失败、取消和过期状态；
- 展示可理解的校验错误、任务错误和组件错误；
- 播放音频并同步高亮当前句段；
- 为同一录音内的本地说话人使用稳定颜色，清楚展示 `unknown`、置信度和风险；
- 下载 JSON、TXT、SRT 和 VTT 结果；
- 支持主动取消任务和删除任务数据。

交付物：Web 应用、可复用交互组件、API client、状态管理、契约 mock、组件测试和浏览器端到端测试。

完成标准：用户能够只通过页面完成输入校验、任务提交、进度查看、同步播放、结果复核、导出和删除；刷新页面后可依据任务 ID 恢复仍在保留期内的任务视图；页面不得把本地 speaker 标签呈现为真实人员身份。

### 6.4 横向验收条件

以下工作贯穿三个里程碑，不另行扩展为第四条产品主线：

- 契约：冻结音频限制、句段 DTO、任务状态、错误码、结果 Schema 和导出格式；
- 数据与安全：路径隔离、文件类型探测、上传和解码资源限制、鉴权、日志脱敏、主动删除和默认保留期；
- 评测与测试：合法脱敏评测集、DER/JER、说话人数准确率、句段归属准确率、`unknown` 覆盖率、长录音和并发压力测试；
- 构建与运行：容器构建、模型交付、CPU 与可选 CUDA 验证、资源限制、监控、告警和操作手册。

M1 完成后可以通过 CLI 验证算法闭环；M2 完成后可以通过 API 验证无人值守任务闭环；M3 与横向验收完成后，才可以把本仓库描述为可交付的完整说话人分离产品。

## 7. 当前明确边界

- 当前可启动 8076、8077 和 8078；M1 CLI 与 M2 统一上传、异步轮询、取消、删除和结果下载均已有实现。
- 当前已有本机 Web 和可选腾讯/阿里 ASR；仍没有数据库队列、多进程/多节点调度、断点续跑或实时推送。已有转写任务重启整任务重跑，云转写任务因凭据不落盘而以 `CREDENTIAL_LOST` 失败。
- 当前不持有人员身份、员工声纹、跨录音参考库或学习数据。
- 当前没有生产部署事实，也没有公网地址。
