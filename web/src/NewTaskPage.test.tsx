import{expect,test}from'vitest';import{validateDocument}from'./NewTaskPage';test('validates transcript contract',()=>{expect(validateDocument(JSON.stringify({schema_version:'voice_analysis_input_v1',segments:[{id:'a',start_ms:0,end_ms:100,text:'x'}]}))).toBe(1)});test('rejects invalid range',()=>{expect(()=>validateDocument(JSON.stringify({schema_version:'voice_analysis_input_v1',segments:[{id:'a',start_ms:5,end_ms:1,text:'x'}]}))).toThrow()})
