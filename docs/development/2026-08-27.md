# 2026-08-27 开发记录

## REQ-001 独立声音分离服务工作区初始化

### 实施基线

- 目标目录原先不存在。
- Smart Badge 当前主链已经退役声纹与本地说话人聚类，只保留 ASR speaker；可复用证据来自历史说明和删除前 Git 对象。
- 本机初始只有系统 Python 3.12，存在 `uv`、Git LFS 和 FFmpeg，没有 Docker 命令。
- Smart Badge 本地环境没有 Hugging Face token；其 LLM、腾讯 ASR 等密钥不属于新推理进程依赖。

### 实际结果

- 建立需求、架构、HTTP、配置、密钥、环境和本地运行文档。
- 建立 `.env.example`、本地被忽略的 `.env`、`config/services.yaml`、模型/音频/日志/临时目录和初始化/验证脚本。
- `.env` 已生成独立 256-bit URL-safe API key；明文没有进入文档、模板或 Git。
- 安装 CPython 3.11.15，建立 `.venv-embedding` 与 `.venv-refine`。
- ECAPA 环境安装 Torch/Torchaudio 2.11.0、SpeechBrain 1.1.0 等 47 个包。
- pyannote 环境首次按 Torchaudio 2.12.0 解析失败，因为当前软件源没有该版本；随后改为成对的 Torch/Torchaudio 2.11.0，安装 pyannote.audio 4.0.7 等 104 个包，并生成两份完整依赖锁文件。
- 初始化脚本在首次失败时暴露出 PowerShell 外部命令错误码未传播和重复建 venv 不幂等，已修正为失败即停止、现有环境不重建。
- 当前 `AGENTS.md` 已从相邻 Smart Badge 仓库原样复制。
- 本地 Git 已以 `main` 初始化；作者身份只写入本仓库的 `.git/config`，没有修改全局 Git 配置，并已创建根提交。

### 验证

- 两个 Python 均为 3.11，Torch/Torchaudio、SpeechBrain、pyannote.audio 和 miniaudio 均完成实际 import/version 检查。
- FFmpeg 可从 PATH 解析；`.env`、配置文件和两套 Python 环境存在。
- ECAPA 与 pyannote 模型目录只有占位文件，验证脚本正确报告两项 `model not-ready`；当前不能声称服务可启动。
- 当前阶段没有服务实现入口，因此没有占用 8077/8078，也没有制造虚假 `/health` 成功结果。

### 未完成与边界

- 模型权重尚未提供；Hugging Face token 为空且在线下载默认关闭。
- 没有实现 HTTP 服务、聚类算法、测试样本或 Smart Badge 接入。
- 本机没有 Docker 命令，本次不建立或验证容器环境。
- 没有触碰 Smart Badge 代码、配置、业务数据或生产环境。
